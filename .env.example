#
# Client-side analytics & captcha configuration
#

VITE_PLAUSIBLE_DOMAIN=bradfordinformedguidance.com
# VITE_PLAUSIBLE_SCRIPT=https://plausible.io/js/script.js
# VITE_PLAUSIBLE_API_HOST=https://plausible.io/api/event

#
# hCaptcha Configuration
#
# IMPORTANT: Get your site key from https://dashboard.hcaptcha.com/
#
# For DEVELOPMENT/TESTING:
# Use hCaptcha's official test key (always passes validation):
VITE_HCAPTCHA_SITEKEY=10000000-ffff-ffff-ffff-000000000001
#
# For PRODUCTION:
# Replace with your actual hCaptcha site key:
# VITE_HCAPTCHA_SITEKEY=your-actual-site-key-here
#
# The site key is public and safe to commit to version control.
# It's used by the browser to render the hCaptcha widget.

#
# Server-side API credentials (set in Vercel project settings)
#

# Ringy lead forwarding credentials
RINGY_ENDPOINT=https://api.ringy.com/v1/lead
RINGY_SID=your-ringy-sid
RINGY_AUTH_TOKEN=your-ringy-auth-token

# JWT secret used for consent receipts
JWT_SECRET=replace-with-long-random-secret

#
# hCaptcha Secret Key (Server-side only)
#
# IMPORTANT: This is your SECRET key - NEVER commit this to version control!
# Set this in your Vercel project settings as an environment variable.
# Get your secret key from https://dashboard.hcaptcha.com/
#
# This key is used server-side to verify captcha responses.
# It must match the site key configured above.
HCAPTCHA_SECRET=your-hcaptcha-secret-key
# HCAPTCHA_VERIFY_ENDPOINT=https://hcaptcha.com/siteverify
